# Supply Unlimited - Environment Configuration
# Copy this file to .env and fill in your actual values

# ===========================
# Django Settings
# ===========================
DEBUG=False
SECRET_KEY=your-super-secret-django-key-change-this-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# ===========================
# Database Configuration (PostgreSQL)
# ===========================
DATABASE_URL=postgresql://postgres:password@db:5432/supply_unlimited_db
DB_NAME=supply_unlimited_db
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_HOST=db
DB_PORT=5432

# Optional: For local PostgreSQL (without Docker)
# DB_HOST=localhost
# DB_PORT=5432

# ===========================
# OpenAI Configuration (Optional - Required for AI Reports)
# ===========================
OPENAI_API_KEY=sk-your-actual-openai-api-key-here

# Optional: Customize AI Model
# OPENAI_MODEL=gpt-4
# OPENAI_TEMPERATURE=0.7

# ===========================
# Redis Configuration (Optional - For WebSocket/Channels support)
# ===========================
REDIS_URL=redis://redis:6379
# Or for local Redis:
# REDIS_URL=redis://localhost:6379

# ===========================
# Email Configuration (Optional)
# ===========================
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# DEFAULT_FROM_EMAIL=noreply@supplyunlimited.com

# ===========================
# Security Settings (Production)
# ===========================
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
# SECURE_HSTS_SECONDS=31536000
# SECURE_HSTS_INCLUDE_SUBDOMAINS=True

# ===========================
# Logging Configuration (Optional)
# ===========================
# LOG_LEVEL=INFO

# ===========================
# IMPORTANT NOTES
# ===========================
# 1. Generate a new SECRET_KEY:
#    python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
#
# 2. For OpenAI:
#    - Get API key from https://platform.openai.com/api-keys
#    - Required for AI Reports feature
#    - Keep it secure, never commit to version control
#
# 3. PostgreSQL:
#    - With Docker: Use 'db' as host (from docker-compose.yml service name)
#    - Local: Use 'localhost'
#
# 4. For production:
#    - Set DEBUG=False
#    - Use strong SECRET_KEY
#    - Enable HTTPS settings
#    - Use environment variables for sensitive data
#
# 5. Never commit .env file - it's in .gitignore
